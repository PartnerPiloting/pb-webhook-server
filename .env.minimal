# MINIMAL LOCALHOST SETUP - COPY ONLY THESE FROM RENDER
# This gets your localhost working with core functionality

# =============================================================================
# TIER 1: ABSOLUTELY REQUIRED (Copy these first)
# =============================================================================
AIRTABLE_API_KEY=
# Choose based on environment:
# Production:   AIRTABLE_BASE_ID=appXySOLo6V9PfMfa
# Staging:      AIRTABLE_BASE_ID=appSTG123example456
# Development:  AIRTABLE_BASE_ID=appDEV123example456  
# Hotfix:       AIRTABLE_BASE_ID=appXySOLo6V9PfMfa (same as production!)
AIRTABLE_BASE_ID=
NODE_ENV=development
PORT=3000

# =============================================================================
# TIER 2: RECOMMENDED (Copy these for full functionality)
# =============================================================================
OPENAI_API_KEY=
PB_WEBHOOK_SECRET=
GCP_PROJECT_ID=
GCP_SERVICE_ACCOUNT_CREDENTIALS_JSON=
RENDER_API_KEY=

# =============================================================================
# TIER 2.5: VERCEL FRONTEND INTEGRATION
# =============================================================================
# These help your localhost backend work with the Vercel frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
NEXT_PUBLIC_WP_BASE_URL=

# =============================================================================
# TIER 3: OPTIONAL (Only if you use these features)
# =============================================================================
GPT_CHAT_URL=
GEMINI_MODEL_ID=
BATCH_CHUNK_SIZE=40
GEMINI_TIMEOUT_MS=900000

# =============================================================================
# QUICK SETUP INSTRUCTIONS
# =============================================================================
# 1. Go to Render Dashboard → pb-webhook-server → Environment
# 2. Copy TIER 1 variables first (4 variables)
# 3. Save this as .env (remove .minimal)
# 4. Run: npm run dev
# 5. Test: http://localhost:3000/basic-test
# 6. If working, copy TIER 2 variables for full features
# 7. VERCEL INTEGRATION: Your frontend at pb-webhook-server.vercel.app will
#    automatically use localhost:3000 when NEXT_PUBLIC_API_BASE_URL is set
#
# =============================================================================
# RESTART AFTER COMPUTER REBOOT - Just say:
# =============================================================================
# "Start the front and back end for my full stack development (render and vercel)"
# 
# This will:
# - Start backend: localhost:3000 (npm run dev in pb-webhook-server/)
# - Start frontend: localhost:3001 (npm run dev in linkedin-messaging-followup-next/)
# - Full-stack local development ready!
#
# =============================================================================
# ENVIRONMENT SWITCHING (Dead Simple - Zero Copy/Paste)
# =============================================================================
# Just say these phrases:
# - "Switch to development mode"    → Uses dev database (safe testing)
# - "Switch to staging mode"        → Uses staging database (major initiatives)
# - "Switch to production mode"     → Uses live database (careful!)  
# - "Switch to hotfix mode"         → URGENT fixes with production data
# 
# I'll automatically:
# ✅ Copy the right .env file
# ✅ Create hotfix branch (if needed)
# ✅ Restart your servers  
# ✅ Tell you which Chrome profile to use
# ✅ Confirm which database you're connected to
# ✅ Warn you about data safety
# ✅ Handle Git merging to prevent losing hotfixes
# ✅ Sync staging with production before major deploys
